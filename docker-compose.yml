version: "3.8"
services:
  slack-bot:
    build: .
    container_name: ts-slack-bot
    environment:
      - NODE_ENV=production
      - TZ=Asia/Seoul
    # Playwright가 필요로 하는 공유 메모리 설정 (옵션)
    shm_size: 1g
